[plugin]
name = "object_yolo"
version = "1.0.0"
type = "object"
description = "Object detection using YOLOE"

[runtime]
command = "uv run python -m object_yolo.main"
idle_timeout = 300
startup_timeout = 30

[capabilities]
actions = ["detect", "detect_batch"]
max_batch_size = 16
